package LinkedList;

public class PalindromeList {

	/*
	 * ListNode head;
	 * 
	 * public int lPalin(ListNode A) { head = A; return lPalindrome(A); }
	 * 
	 * private int lPalindrome(ListNode A) { if (A == null) { return 1; } int retval
	 * = lPalindrome(A.next); if (retval == 0) { return 0; } if (head.val == A.val)
	 * { retval = 1; } else { retval = 0; } head = head.next; return retval; }
	 */

}
