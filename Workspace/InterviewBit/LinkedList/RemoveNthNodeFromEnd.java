package LinkedList;

public class RemoveNthNodeFromEnd {
	/*
	 * public ListNode removeNthFromEnd(ListNode A, int B) { int len = 0; ListNode
	 * temp = A; while(temp!=null){ len++; temp=temp.next; } int nthFromStart = len
	 * - B + 1; if(nthFromStart<=1){ return A.next; } temp=A; int i = 0;
	 * while(temp!=null){ i++; if(i==(nthFromStart-1)){ temp.next=temp.next.next; }
	 * temp=temp.next; } return A; }
	 */
}
